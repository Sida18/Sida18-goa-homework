import React from "react";
import { Outlet } from "react-router-dom";

export default function Book1(){
    const {id} = useParams()
    return(
        <div>
            book1
        </div>
        
    )
  
}